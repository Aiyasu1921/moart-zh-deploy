# 《透视步进尺》实现计划书 —— 基于 MoArt-aiyasu

> 版本：v0.2（v0.1 + 数学验证结果 + 分支/更名/版本重置 + 仅透视线模式）  
> 日期：2026-08-03  
> 项目：**MoArt-aiyasu**（开发分支 `moart-aiyasu`，版本号从 **ver1** 重新计）  
> 相关文件：
> - 原型：`D:\MyArticles\VsCodeArticles\PerspCalc\PerspCal_v0.0.2.py`（及 `透视计算器1.0.py`、`cal_demo_240506_001.py`）
> - 宿主：`D:\MyArticles\VsCodeArticles\别人的库\MoArt-ver1248\moart-zh-deploy\index.html`（根目录 `moart-ver1248.html` 与之字节一致，MD5 均为 `E1C51450C0AEF4AB6D81254055216EA8`，开发时以一份为基线，改完同步另一份）
> - 数学验证：`persp-math-test/verify_persp_ruler.py`（P0 已执行，全部通过）

---

## 1. 背景与目标

二维动画里做“纵深方向的走路”是最难画的透视之一：角色每走一步，位置和大小都要按透视规律变化。`PerspCal_v0.0.2.py` 证明了一个最小可行方案：输入画布、物体尺寸、物距、焦距后，按固定步长逐帧推算出物体在画面中的尺寸和缩放比。但它只算“尺寸”，不算“位置”，而且只支持正对镜头前后走。

本项目的目标，是把这套计算搬进 MoArt-aiyasu 画布：

1. 用“透视线 → 灭点”的几何构造直接在现场算出焦距、物距和三个方向的视场角；
2. 用“角色矩形 + 实际身高”校准世界单位；
3. 用“步长 + 方向线”生成一串角色矩形，覆盖纵深、斜向、折线行走；
4. 生成的辅助框只作参考（不落墨、不进图层），可随时显示/隐藏/清除，并随项目保存。

**两种用法自由选择**（用户需求）：

- **仅透视线模式**：拉完透视线、看完灭点/视场圆/FOV/焦距后即可停手，不进入步进计算；
- **完整步进模式**：在第 1 步之后继续角色校准 + 步长路径，生成走路矩形序列。

---

## 2. 原型理解（PerspCal_v0.0.2.py）

### 2.1 数学模型（小孔成像 / 35mm 等效）

```
输入：
  画布宽高        canvas_wide × canvas_high    (px)
  物体宽高        object_wide × object_high    (cm)
  物距            u                            (cm)
  焦距            f                            (mm)
  步长            i                            (cm，靠近为正/远离为负)

胶片等效：
  film_wide = 36 mm
  film_high = 36 / canvas_wide * canvas_high   (mm)

视场角：
  fov_wide = 2 * atan( (film_wide / 2) / f )
  fov_high = 2 * atan( (film_high / 2) / f )

像尺寸（px）：
  x = canvas_wide / fov_wide * atan( object_wide / u )
  y = canvas_high / fov_high * atan( object_high / u )

步进：
  每走一步：u -= i，重新计算 x、y，输出
  变化量 Δx/Δy、变化比 x₂/x₁×100%
```

### 2.2 功能要点

- 这是**角度投影近似**：像像素 = 画布像素 / 该方向视场角 × 物体的角直径 `atan(尺寸/物距)`。
- 已知四要素 {焦距 f、物距 u、物体尺寸、画面占比} 中任意三个，可反推第四个（见 §3.3）。
- 步进循环输出：第 n 步的像宽/像高、与上一步的像素差、相对初始尺寸的百分比。
- 交互：命令行逐步回车，可随时停止、重新开始。

### 2.3 局限（本计划要补齐的）

| 局限 | 本计划的补法 |
| --- | --- |
| 只算尺寸变化，不算画面内位置 | 从透视构造重建虚拟相机，中心投影算出每步的脚下位置与矩形四角 |
| 只支持正对镜头前后走 | 将步长向量分解到世界坐标轴，支持斜向/折线路径 |
| 焦距、物距全靠手输 | 由灭点三角形 + 垂心圆直接反推 f 与 u |
| 画布尺寸、物体尺寸、物距互不校验 | 四要素互推面板 + 角色矩形校准 |
| 命令行交互，看不到画 | 全部在画布上以覆盖层交互 |

---

## 3. 透视尺功能梳理

### 3.1 灭点三角形与垂心圆（90° 视场圆）

按用户描述，三点透视的几何构造如下（已用数值实验验证，见下方结论）：

1. 三个灭点 V₁、V₂、V₃ 在投影平面（画面）上构成三角形；
2. 取三角形垂心 H（经典透视学中，**垂心即主点**——视轴在画面上的投影点）；
3. 过 H 分别作三条与对应边平行的线 L₁ ∥ V₂V₃、L₂ ∥ V₃V₁、L₃ ∥ V₁V₂；
4. 在每条平行线上取点 Pᵢ，使 Pᵢ 与**该边的高**（对边顶点 + 该边垂足为直径）构成以 Pᵢ 为直角顶点的直角三角形：即 Pᵢ 是“以该边高为直径的 Thales 圆”与该平行线的交点（每边可交 0~2 个点；灭点三角形必交 2 个）；
5. 这三个直角顶点 P₁、P₂、P₃ **共圆，圆心为 H**，该圆代表 90° 视场角的范围；
6. 圆半径 r 即焦距 f（画面像素单位），用 r 与画面对角线、高、宽的相对长度换算出三个方向的视场角。

**已验证结论（2026-08-03，`persp-math-test/verify_persp_ruler.py`，全部通过，误差 ≤ 5e-13）**：

- **共圆性（任意三角形成立）**：三边共 6 个交点全落在以 H 为圆心的同一圆上，且 `r² = d·(h−d)`（d = H 到该边的距离，h = 该边的高，即高被 H 分割的两段之积，三边相等）。验证 1122 个随机三角形全部通过；
- **垂心 = 主点**：用真实相机（小孔成像）随机生成 239 个灭点三角形，垂心与主点偏差 ≤ 8e-15；
- **焦距反解**：H 到三灭点的向量两两内积恒等于 `−f²`，由此可从任意灭点三角形反解焦距 f；
- **90° 视场圆**：上述构造圆的半径 **r = f**（239/239 通过）——这就是“该圆代表 90° 视场角”的数学含义；
- **FOV 公式**：`FOV = 2·atan(画面该方向半长 / r)`——水平用画布宽/2，垂直用高/2，对角线用对角线/2（与圆直径的比即 `2·atan(方向长度/圆直径)`）；
- **灭点距离**：`dᵢ = f · tan φᵢ`（φᵢ 为该世界方向与视轴的夹角）；
- **35mm 等效焦距**：`f_mm = 36mm × r_px / 画布宽_px`；
- **原型公式对照**：像尺寸公式、反解物距、反解焦距与 `PerspCal_v0.0.2.py` 完全一致（误差 0）。

> 注：以上结论中 FOV 公式的前提是主点位于画面中心（对称构图）；主点偏离中心时需按 H 到四边距离分别计算左右/上下 FOV（见风险 R1）。

### 3.2 四要素互推（焦距 / 物距 / 尺寸 / 占比）

由原型公式改写（已知任意三个求第四个）：

```
画面占比 s = 画布像素/视场角 × atan(物体尺寸 / u)      （原型正用）
物距     u = 物体尺寸 / tan(s × 视场角 / 画布像素)
物体尺寸   = u × tan(s × 视场角 / 画布像素)
视场角     = 画布像素 × atan(物体尺寸/u) / s
焦距     f = film / (2 × tan(视场角/2))
```

典型工作流（用户原话的落点）：

> 先有构图 → 定下物体在画面中的占比 → 由透视线/垂心圆定焦距 → 由设定知物体尺寸 → 反推物距 u。

### 3.3 纵深 / 斜向步进

- **纵深步进**：直接复用原型逻辑 `u_k = u₀ ∓ k·步长`，每步算尺寸缩放比；新增强的是**位置**——把角色脚底点沿世界深度轴移动 k·步长，做中心投影得画面坐标。
- **斜向 / 折线步进**：把步长向量分解到三个世界方向（即灭点三角形对应的三个正交世界轴），逐分量投影相加。数学内核基于“从透视构造重建虚拟相机”：视点 E 位于主点 H 后方距离 f 处，视轴 ⊥ 画面；世界坐标 ↔ 图像坐标之间是一个标准中心投影，校准一次后所有步点都是闭式解。

---

## 4. 需求与交互设计

### 4.1 三步输入流程（用户构想）

```
第 1 步：透视线 → 灭点
  用户在画布上拉出 ≥2 组透视线（每组 ≥2 条场景中的平行线），
  自动求交点得 ≥2 个灭点；第三个灭点由第三组线给出，
  或由 UI 提供“正交补全”（见风险 R2）。
  → 显示灭点三角形、垂心、90° 视场圆、三方向 FOV、焦距 f。
  └─ 决策点（用户自由选择，可随时反悔）：
       A. 完成（仅透视线）——保留透视线/灭点/视场圆与读数，不进入步进；
       B. 继续步进 —— 进入第 2、3 步。

第 2 步：角色校准
  用户拖出一个“身高 × 肩宽”的矩形（初始按默认比例），
  并输入实际身高（如 160 cm）。
  → 系统自动换算肩宽实际值，并给出该位置对应的物距 u。

第 3 步：步长与路径
  用户输入步长（如 60 cm）与步数（或由方向线长度折算），
  在画布上拖出走的方向和距离（支持折线、斜向）。
  → 沿路径生成一串角色矩形（每步一个：位置 + 透视尺寸）。
```

### 4.2 输出内容

- 角色矩形序列：每步一个，标注序号；
- 辅助元素：透视线（半透明）、灭点标记、三角形、垂心、90° 圆、路径线、步点；
- 数值面板（HUD / 侧栏）：
  - FOV：水平 / 垂直 / 对角线（度）
  - 焦距 f（35mm 等效 mm + 纸面像素 px）
  - 当前物距 u（cm）
  - 角色实际身高 / 肩宽（cm）
  - 每步的序号、画布尺寸（px）、相对首步缩放比（%）

### 4.3 交互状态机

```
off
 └─ 点击“透视尺”工具 → guide
guide
 ├─ 子状态 captureLines：拉透视线（≥2 组）→ compute 灭点/圆/FOV → showPanel
 │    └─ 决策点：完成（仅透视线 → stage='linesDone'）或 继续（→ calibCharacter）
 ├─ 子状态 calibCharacter：拖矩形 + 输身高 → u、肩宽
 ├─ 子状态 walkPath：输步长/步数 + 拖方向线 → 生成矩形序列
 ├─ 任意子状态可 redo / 重设 / 隐藏辅助层 / 清除
 ├─ 仅透视线完成后，面板保留“继续步进”入口（随时可补第 2、3 步）
 └─ 再次点击工具或 Esc → off（数据保留，仅隐藏）
```

### 4.4 边界与异常

- 只有 1 组透视线（平行线不收敛/退化灭点）：提示“至少两组”。
- 2 点透视（只有 2 个灭点）：进入“正交补全”或要求补第三组线。
- 灭点落在画面内很远处（近似平行）：仍按几何计算，数值异常时给出提示。
- 步数过多（>200）：矩形序列只绘制可见区域 + 常量屏幕宽度线宽（性能，见风险 R5）。
- 相机（camera keyframes）移动时：辅助框按纸面空间跟随（见风险 R4）。

---

## 5. MoArt-aiyasu 技术现状（改造基础）

经通读 `moart-zh-deploy/index.html`（5.1MB 单文件、无构建步骤，直接编辑 HTML），关键事实：

| 主题 | 现状 | 对实现的意义 |
| --- | --- | --- |
| 工具系统 | `PAINT_DRAW_TOOL_IDS = ['pen','eraser','line','curve','rect','ellipse','lasso-fill','bucket']`；`setActivePaintTool(id)` 统一分发；形状按钮用 `data-paint-shape`，编辑工具用 `data-paint-edit-tool`；选项面板按 `[data-paint-inline=...]` 分节，`updatePaintUI()` 刷新激活态 | 新增“透视尺”可作为**引导型工具**：挂到工具条新按钮 + 自己的 inline 面板 |
| 画布坐标系 | 纸面 `CONTE_PAPER_WIDTH=561` × `CONTE_PAPER_HEIGHT=396`，规范坐标 norm 0..1；`conteNormToLayerX/Y()` 换算 | 灭点/矩形都存 norm 坐标，缩放平移自动正确 |
| 覆盖层渲染 | `#videoPaintStageOverlay` 全屏 canvas，`drawPaintStageOverlay()` 统一绘制（选框/变换框/印章预览），在 pan/zoom/flip 钩子里重绘；有 `hasContent` 快路径 | 透视尺全部画在这层，需要把 `perspGuide` 加入 `hasContent` 判断与重绘钩子 |
| 笔画系统 | 落墨走 `beginPaintStroke` → `state.paintCurrentStroke` → `page.strokes`，进撤销栈 | **透视尺不该落墨**，用独立状态机 + 覆盖层，避免污染撤销栈 |
| 全局状态 | 单一 `state` 对象，`state.project`（`createInitialProject()`） | 运行时数据放 `state.perspGuide` |
| 持久化 | `projectForJsonStorage(opts)` 克隆 project + 写 `schemaVersion`（`PROJECT_FORMAT_VERSION`）；IndexedDB 自动保存 + JSON 导入导出 + localStorage 设置 | 在 project 上加 `perspGuide` 字段，读旧文件时缺省回退 |
| i18n | `I18N.dict`（zh/ja/ko/en 四语），静态 `data-i18n`，动态 `t()`，`applyI18n()` | 新面板词条全部进 dict，zh 为主 |
| 版本 | 更名 **MoArt-aiyasu**，版本号重置为 `<title>MoArt-aiyasu ver1`，`APP_NAME='MoArt-aiyasu'`、`APP_DISPLAY_VERSION='1'`；版本信息对话框按 title 自动识别 | 新功能按 ver1 起记，延续 MoArt 版本信息惯例 |

---

## 6. 技术方案

### 6.1 数据模型

```js
// 运行时（state）
state.perspGuide = {
  active: false,            // 工具是否开启
  visible: true,            // 辅助层显示开关
  stage: 'off',             // 'captureLines' | 'linesDone'(仅透视线) | 'calibCharacter' | 'walkPath' | 'done'
  linesOnly: false,         // true = 用户选择仅透视线模式，跳过步进（数据保留，可随时继续）
  lines: [],                // 用户拉的透视线 [ {a:{x,y}, b:{x,y}, groupId} ]（norm 坐标）
  vps: [ {x,y}, ... ],      // 灭点（norm，可 2~3 个）
  h: {x,y},                 // 垂心（主点）
  circle: {cx, cy, r},      // 90° 视场圆
  fov: {h, v, d},           // 三方向视场角（度）
  focalPx: 0, focalMm: 0,   // 焦距（纸面 px / 35mm 等效）
  character: {              // 角色校准
    box: {x,y,w,h},         // 画布上拖出的矩形（norm，脚底在 box.y+h）
    heightCm: 160,          // 用户输入的实际身高
    shoulderCm: 0,          // 由比例换算
    uCm: 0                  // 由占比反推的物距
  },
  walk: {
    stepCm: 60,
    path: [ {x,y}, ... ],   // 方向线（norm 折线）
    steps: [ {x,y,w,h,idx,u,scale} ] // 生成的矩形序列
  }
};

// 持久化：projectForJsonStorage 中
copy.perspGuide = state.perspGuide;   // 仅在存在且有内容时写入
// 加载：project.perspGuide 缺省为 null → 一切 UI 回退，旧文件零影响
```

### 6.2 计算内核（独立纯函数模块）

建议组织为文件内一个 `PerspMath` 命名空间（纯函数，不碰 DOM/state），便于先做数学验证：

```js
const PerspMath = {
  lineIntersect(l1, l2),        // 两条透视线求灭点
  triangleOrthocenter(p1,p2,p3),
  sideParallelThrough(p, a, b), // 过点作边的平行线
  rightAnglePointsOnLine(line, altitude), // Pᵢ：“以该边高为直径的圆”∩ 平行线（已数值验证）
  orthoCircle(p1,p2,p3),        // 垂心圆（90° 视场圆）
  fovFromCircle(circle, canvasW, canvasH), // 三方向 FOV（P0 验证项）
  focalFromVps(h, vp, filmMm),  // d = f·cotφ → f
  screenRatio(f, u, size),      // 原型公式
  solveU(f, size, ratio),       // 四要素反推
  solveF(u, size, ratio),
  depthStepSeries(u0, stepCm, n),   // 纵深步进（对照 Python 原型）
  walkProjection(guide, worldStepVec) // 斜向步进：世界→画面
};
```

### 6.3 工具注册与 UI

1. 工具条：在 `#paintToolStrip` 增加一个 `data-paint-guide="persp"` 按钮（图标用三点透视示意），并接入 `setActivePaintTool`（新增保留字 `persp-guide`，不进入 `PAINT_DRAW_TOOL_IDS`，走独立分支或编辑工具分支）。
2. 选项面板：新增 `[data-paint-inline="persp"]` 分节，含：
   - 状态提示（第 1/2/3 步引导文案）
   - 数值输入：身高 cm、步长 cm、步数
   - 数值读数：FOV 三向、f、u、每步比例
   - 命令按钮：完成、清除、显示/隐藏
3. `updatePaintUI()` 同步按钮激活态与 inline 面板显隐。
4. i18n：`I18N.dict` 新增 `persp.*` 词条（zh 主、ja/ko/en 同加），静态用 `data-i18n`，动态数值用 `t()`。

### 6.4 渲染（覆盖层）

- 所有透视尺图形在 `drawPaintStageOverlay()` 中追加一个 `drawPerspGuide(ctx, ...)` 分支；
- 把 `state.perspGuide.active || state.perspGuide.stage !== 'off'` 并入 `hasContent` 快路径，避免被提前 clear；
- 重绘触发：现有 pan/zoom/flip 钩子已调 `drawPaintStageOverlay()`；每次透视线/矩形/路径拖拽中与结束时显式调用；
- 样式规范：
  - 透视线：半透明虚线（`setLineDash`，参考现有 marquee 的 `strokePaintOverlayPath`）；
  - 灭点：十字 + 标签 V₁/V₂/V₃；垂心与 90° 圆：强调色；
  - 角色矩形：实线框 + 脚底线；步进序列：实线框 + 序号；
  - 线宽随视图缩放保持常量屏幕宽度（参考 `paintMarqueeStyle(1).view` 的 scale-aware 处理）。

### 6.5 输入状态机

- 独立 pointer 管道（不在 `beginPaintStroke` 落墨路径里），参考 `beginPaintTransformDocument` 的 drag 处理模式：`pointerdown/move/up` + `setPointerCapture`；
- 第 1 步：连续两次 pointer 拖拽定义一组透视线（起点→终点，吸附 15° 增量可复用 line 工具习惯）；组内 ≥2 条后求交点；≥2 组后进入计算；
- 第 2 步：拖拽矩形（Shift 锁定肩宽比，Alt 从中心拖，与 rect 工具习惯一致）；
- 第 3 步：先输步长/步数，再拖方向线（首段锚定起点，后续点击续段形成折线，双击/Enter 结束）；
- Esc / 再次点击工具：退出当前子状态（数据保留）；“清除”按钮：全量清空。

### 6.6 持久化与兼容

- 项目 JSON：`copy.perspGuide`（norm 坐标 + 所有输入参数），保存/加载/自动保存全链路生效；
- 兼容：加载时 `project.perspGuide == null` → 按未启用处理；不参与任何旧字段迁移；
- 进 undo 栈：**默认不进**（参考性辅助层）；“清除”本身也不产生撤销记录，避免用户误以为清除可撤销后丢失参考框。若后续要支持，可为 `perspGuide` 加专用 undo 类型。

### 6.7 与现有功能的关系

- 与“透视变换”（`data-paint-edit-tool="perspective"`，对图层四角做仿射/透视变换）无关，不冲突，命名上注意区分（建议 UI 上叫“透视尺/步进尺”，避免与既有“透视变换”混淆）；
- 与洋葱皮、时间轴无耦合：辅助框是纸面空间数据，画在哪个关键帧都能看到（P2 可考虑按帧/按镜头绑定，见里程碑）；
- 不影响导出：辅助层只画在 stage overlay，不进入图层位图与导出。

---

## 7. 实现里程碑与验收标准

| 阶段 | 内容 | 验收标准 |
| --- | --- | --- |
| **P0 数学验证** | ✅ 已完成（2026-08-03）：`persp-math-test/verify_persp_ruler.py` 验证共圆性、垂心=主点、两两内积=−f²、圆半径=f、FOV=2·atan(半长/r)、d=f·tanφ、原型公式对照 | 全部通过（误差 ≤ 5e-13），结论见 §3.1；JS `PerspMath` 实现后需与此脚本结果 1:1 对照 |
| **P0.5 几何可视化** | 透视尺工具雏形：拉透视线 → 灭点 → 三角形/垂心/90° 圆/三向 FOV/f 的覆盖层渲染 + 数值面板；第 1 步后提供“完成（仅透视线）”与“继续步进”两个出口 | 画布上构造任意三点透视，图形与手算一致；缩放平移后覆盖层不漂移；仅透视线模式可正常退出并保留辅助层 |
| **P1 角色校准与物距** | 角色矩形拖拽 + 身高输入 → 肩宽、物距 u；四要素互推面板 | 输入 160cm、拖出的框高 320px（画布 561 高）→ u 与公式手算一致 |
| **P1.5 纵深步进** | 步长 + 步数 → 纵深矩形序列；逐项与 Python 原型缩放比一致 | 任意 u₀、步长、步数下，JS 序列与 Python 输出 1:1 |
| **P2 斜向/折线步进** | 方向线（含折线）→ 虚拟相机投影生成矩形序列；i18n 全词条；项目保存/加载 | 斜向路径下矩形位置/大小连续无跳变；保存-重开数据完整 |
| **P2.5 打磨** | 吸附、常量线宽、性能优化、异常提示、帮助文案 | 500 步序列拖动仍流畅；错误场景有明确提示 |
| **P3 可选增强** | 与洋葱皮/时间轴联动（按帧显示）、导出辅助线 PNG、步长方向吸附地面透视线 | 按需求单确认 |

---

## 8. 测试方案

### 8.1 数学验证（先行，P0）

> ✅ 已完成（2026-08-03）：见 `persp-math-test/verify_persp_ruler.py` 与 §3.1 已验证结论。
> 剩余工作：JS `PerspMath` 落地后，把该脚本移植为 JS 版本做 1:1 对照回归。

1. **几何不变量**：随机生成 1000 组三点透视三角形，断言：
   - 三 Pᵢ 到 H 距离相等（共圆）；
   - Pᵢ 与对应边两端点构成直角；
   - 垂心、圆心数值与解析解一致。
2. **FOV 定量关系**：对“90° 圆 ↔ 三方向 FOV”构造已知场景（设定 f、视场角 → 反推灭点位置 → 正向计算圆），确认候选公式的常数；若不符合则记录修正公式。
3. **与原型对照**：同一组 {画布、物体尺寸、u、f、步长}，JS `PerspMath` 与 Python 输出逐行 diff。

### 8.2 UI/回归测试

- 工具切换：透视尺 ↔ 画笔/形状/变换各工具互不干扰，切换后覆盖层正常；
- 坐标系：缩放、平移、水平/垂直翻转后辅助图形与纸面对齐；
- 持久化：保存 JSON → 重开 → 数据完整；用旧版本 JSON 打开无报错；
- 语言：zh/ja/ko/en 四语切换后面板文案完整；
- 输入设备：鼠标 + 触控笔 + 触屏（双指平移不误触透视尺）；
- 回归：无透视尺数据时，原有绘制/导出/撤销行为零变化。

---

## 9. 风险与开放问题

| 编号 | 风险 / 问题 | 应对 |
| --- | --- | --- |
| R1 | ~~“90° 视场圆 ↔ FOV”换算公式未定~~ **已解决**：`r = f`、`FOV = 2·atan(半长/r)`、`f² = −两两内积`，见 §3.1 验证结果 | 剩余注意：主点偏离画面中心时，FOV 需按 H 到各边距离分别计算（对称构图时才等于“半宽/半高”公式）；JS 实现时补一个非对称用例 |
| R2 | 只有 2 个灭点（两点透视）时，第三个灭点如何确定 | 方案 A：要求第三组线；方案 B：假设竖直方向 ⊥ 两灭点连线（地平线法），自动补全；两种都做，UI 可切换 |
| R3 | 斜向步进的“世界 ↔ 图像”映射若不严谨，矩形序列会漂 | 采用“虚拟相机重建”（视点 E、焦距 f、视轴 ⊥ 画面、世界轴对齐灭点方向），全部步点闭式解；对照 P1.5 纵深结果做自洽性检验 |
| R4 | 相机关键帧（camera keyframes）移动时辅助框语义 | 先定“纸面空间”语义（随纸面移动），P3 再评估是否需要屏幕空间模式 |
| R5 | 大场景 + 数百步矩形的覆盖层性能 | 只绘制可见区域；线宽用常量屏幕宽度；必要时把辅助层降采样/冻结 |
| R6 | 单文件 5MB 手工修改回归风险 | 以 moart-zh-deploy 为唯一基线，每阶段用 `git diff` 审查；两个 HTML 文件改完再同步（当前字节一致） |
| R7 | 与现有“透视变换”工具命名/心智混淆 | UI 名用“透视步进尺”，帮助文案里注明与图层面板透视变换的区别 |

---

## 10. 开发锚点（代码位置速查）

以下均在 `moart-zh-deploy/index.html` 中（5MB 单文件，用关键词搜索）：

| 目的 | 锚点 |
| --- | --- |
| 工具分发 | `PAINT_DRAW_TOOL_IDS` / `function setActivePaintTool` |
| 形状按钮与选项面板 | `data-paint-shape` / `[data-paint-inline="..."]` / `function updatePaintUI` |
| 覆盖层 | `function ensureVideoStageOverlayCanvas` / `function drawPaintStageOverlay` / `strokePaintOverlayPath` / `paintMarqueeStyle` |
| 坐标换算 | `CONTE_PAPER_WIDTH` / `conteNormToLayerX` / `conteLayerToFrameX` |
| pointer 管道参考 | `beginPaintStroke` / `beginPaintTransformDocument` / `continuePaintStroke` |
| 全局状态 | `const state = {` |
| 持久化 | `function projectForJsonStorage` / `PROJECT_FORMAT_VERSION` / `applyRestoredIndexedDbProject` |
| i18n | `const I18N = { dict: {` / `function t(` / `function applyI18n` |
| 版本信息 | `versionInfoVersionValue`（title 自动识别 `verNNNN`） |

---

## 11. 建议的下一步

1. ✅ 数学验证已完成（`persp-math-test/verify_persp_ruler.py`），R1 公式已定死；
2. 下一步在 HTML 里做“静态渲染验证”：写死一组灭点/圆/矩形，验证 `drawPaintStageOverlay` 的渲染与缩放对齐；
3. 渲染通过后，按 P0.5（仅透视线模式优先）→ P1 → P1.5 → P2 的顺序接交互与持久化。
